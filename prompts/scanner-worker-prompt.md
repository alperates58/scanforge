# Scanner Worker Prompt

Implement scanner worker with safe defaults.

Requirements:
- Job-based execution
- Tool adapters
- Timeout per command
- Rate limits
- JSON artifact storage
- Normalization to common finding schema
- Technology-to-scanner matrix
- No destructive scans
- Verified domains only for active scans

Start with mock adapters if tools are not installed, but keep contracts real.
